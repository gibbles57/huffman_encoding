# Huffman Encoding

A program to encode and decode via the Huffman algorithm. 

## Features
- Hash table to count occurrences of encoding results
- Priority queue to ensure peak optimization of the greedy algorithm
